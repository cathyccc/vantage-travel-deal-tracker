"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import { passengerSchema } from "@/lib/schemas/booking";

export default function PassengerForm () {
  const router = useRouter();
  
  return (
    <div>
      some form
    </div>
  )  
}